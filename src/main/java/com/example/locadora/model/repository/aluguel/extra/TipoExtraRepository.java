package com.example.locadora.model.repository.aluguel.extra;

import com.example.locadora.model.entity.aluguel.extra.TipoExtra;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TipoExtraRepository extends JpaRepository<TipoExtra, Long> {
}
